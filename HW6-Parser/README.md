Запуск осуществляется с помощью `Application`. 
На вход принимается арифметическое выражение, 
на выходе две строчки: список токенов в ОПЗ и результат вычисления.
Либо можно воспользоваться Тестами `AppTest`